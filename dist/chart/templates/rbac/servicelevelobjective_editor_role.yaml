{{- if .Values.rbac.enable }}
# This rule is not used by the project slo-custom-ressource itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the sre.slo.romanmasson.dev.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    {{- include "chart.labels" . | nindent 4 }}
  name: servicelevelobjective-editor-role
rules:
- apiGroups:
  - sre.slo.romanmasson.dev
  resources:
  - servicelevelobjectives
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - sre.slo.romanmasson.dev
  resources:
  - servicelevelobjectives/status
  verbs:
  - get
{{- end -}}
